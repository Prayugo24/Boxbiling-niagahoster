# For Instalation
```bash
    # get into the project
    - cd BoxBilling-niagahoster
    # Running docker compose
    - docker-compose up -d
    # import database to mysql
    - docker exec -i [CONTAINER_ID] mysql -uroot -p[MYSQL_ROOT_PASSWORD] [DB_NAME] < [PATH_DATABASE]/niagahoster.sql
```


